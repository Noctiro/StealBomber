{
    "configurations": [
        {
            "command": "npm start",
            "name": "Run Electron",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/web"
        }
    ]
}